# .NET
bin/
obj/
*.user
.vs/

# Node
# routeTree.gen.ts is COMMITTED on purpose: router.tsx imports it, so a fresh
# checkout fails `pnpm typecheck` until someone runs a build. The Vite plugin
# regenerates it on dev/build, so a stale one shows up as a diff.

node_modules/
dist/
.tanstack/

# Local env & secrets
.env
.env.*
!.env.example
*.local.json
.claude/settings.local.json

# OS / editor
.DS_Store
.idea/
src/Premise.Api/Internal/Generated/
